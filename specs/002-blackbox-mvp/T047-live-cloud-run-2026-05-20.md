# T047 — Deprecated Live Cloud Integration Run

This was a provider-specific integration-run artifact from the pre-GCP/Sthrip
production posture. The detailed provider commands, env var names, endpoint
names, and account identifiers were removed to avoid future operational
confusion.

Current live scan VM work should follow the GCP rail documented in
`docs/project-current-context.md`, `server/.env.example`, and
`docs/runbooks/blackbox-mvp-operator.md`.
