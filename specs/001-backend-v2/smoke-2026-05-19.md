# T102 Live Smoke — DEFERRED

**Date**: 2026-05-19
**Status**: ⏸ PAUSED — production infrastructure not available in driver env
**Task**: T102 (`specs/001-backend-v2/tasks.md` line 174)
**Acceptance (from spec)**: live scan completes end-to-end with at least one finding

## Decision

Execution paused per loop rule 5 (external preconditions missing). Code-level
readiness is verified (see "Code-Level Readiness" below); only live
infrastructure provisioning is blocked.

## Preflight Check Results (2026-05-19, driver env)

| Precondition | Status | Detail |
| --- | --- | --- |
| `HETZNER_API_TOKEN` | ❌ EMPTY | required for VPS provisioning |
| `HETZNER_SSH_KEY_NAME` | ❌ EMPTY | required for cloud-init SSH access |
| `TENSOL_VPS_AGENT_IMAGE` | ❌ EMPTY | image built in T074 Dockerfile but never built/pushed to a registry |
| `TENSOL_WEBHOOK_BASE_URL` | ❌ EMPTY | no public-reachable backend host with TLS |
| `RESEND_API_KEY` (or alt email) | ❌ EMPTY | magic-link emit defaults to stdout but no auth-proof to outbound recipient |
| Docker daemon | ❌ DOWN | `Cannot connect to the Docker daemon at unix:///Users/saveliy/.docker/run/docker.sock` |
| Published `vps-agent` image | ❌ MISSING | not in local cache; not pushed to any registry |
| Scan target ownership | ⚠ UNCLEAR | `example.com` (IANA-reserved) — auth-proof verification (DNS TXT / well-known file) is impossible without domain control |

**Verdict**: 7 of 8 preconditions unmet. Live smoke cannot proceed from a
local driver workstation.

## Code-Level Readiness (verified prior to deferral)

The build itself is ready for live execution; only deployment is blocked.

| Evidence | Result | Source |
| --- | --- | --- |
| Server suite | 434 pass / 0 fail | T099/T100 |
| vps-agent suite | 45 pass / 0 fail | T099/T100 |
| Coverage gate | 93.92% (≥85% threshold) | T100 `f0bcb8c` |
| Playwright E2E first-scan | PASS (hybrid UI+API) | T090 `498fe7c` |
| Audit chain verifier | golden fixture + CLI smoke pass | T101 `4783ffe` |

## Resume Checklist

Before un-pausing T102, the following must be true:

- [ ] Acquire `HETZNER_API_TOKEN` (Hetzner Cloud project with quota + billing).
- [ ] Upload a passwordless SSH key to that Hetzner project; set
      `HETZNER_SSH_KEY_NAME` to its label.
- [ ] Bring up local Docker Desktop daemon **OR** use a remote Buildx context;
      build `apps/vps-agent/Dockerfile` and push to a registry, e.g.
      `ghcr.io/ageree/tensol-vps-agent:0.1.0`. Set `TENSOL_VPS_AGENT_IMAGE` to
      the pushed tag.
- [ ] Deploy the `apps/server` build to a public host with TLS + DNS (no
      preference; any Linux VPS with a Caddy/Traefik reverse proxy works).
      Set `TENSOL_WEBHOOK_BASE_URL` to that origin (`https://…`).
- [ ] Identify a scan target with auth-proof control:
      - Option A: own domain (recommended — controls DNS TXT record).
      - Option B: HTTP `/.well-known/tensol-auth-proof.txt` on a reachable
        host the operator controls.
      - `example.com` is **not** suitable; IANA owns it.
- [ ] Set `RESEND_API_KEY` (or `EMAIL_PROVIDER=stdout` if magic-link should
      print to server log only for the smoke).
- [ ] Confirm Hetzner image + region matches `apps/server/src/hetzner/`
      defaults (`ubuntu-22.04`, region from spec).
- [ ] Re-read `specs/001-backend-v2/quickstart.md` step-by-step at execution
      time.

## Run Recording Template (fill in on resume)

When T102 is un-paused, append a new section below this line.

```
## Live Run — YYYY-MM-DD

- Operator:
- Backend host:
- Scan target:
- Engagement profile:
- VPS provisioned at: HH:MM:SS UTC
- VPS terminated at:  HH:MM:SS UTC
- Wallclock:          MM:SS
- Hetzner cost (€):
- Findings emitted:
- Audit chain verified: yes / no (CLI output excerpt)
- Notes / surprises:
```

## References

- Task definition: `specs/001-backend-v2/tasks.md` (line 174, T102)
- Quickstart steps: `specs/001-backend-v2/quickstart.md`
- VPS agent: `apps/vps-agent/`
- Webhook contract: `specs/001-backend-v2/contracts/webhook.md`
- Audit chain verifier (T101): `apps/server/src/audit/verify-chain.ts`
