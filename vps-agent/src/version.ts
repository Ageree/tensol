/**
 * Build identity. Updated automatically by CI workflow.
 * Initial pin so we have a published image to reference in deploy.
 */
export const VPS_AGENT_VERSION = "0.1.0";
export const VPS_AGENT_GIT_SHA = "build-time"; // overridden by CI build-arg in future
